<div class="container">
  <div class="mx-auto col-12">
    <div class="row">
      <h4 class="mt-3 col-12">商品詳細</h4>
        <div class=col-4>
          <%= image_tag @item.get_image(100,100) %>
        </div>
        <%= form_with model: @item, url: admin_item_path(@item.id) do |f| %>
          <table class="table">
            <tbody>
              <tr></tr>
              <tr>
                <td>商品名</td>
                <td><%= @item.name %></td>
              </tr>
              <tr>
                <td>商品説明</td>
                <td><%= @item.introduction %></td>
              </tr>
              <tr>
                <td>ジャンル</td>
                <td><%= @item.name %></td>
              </tr>
              <tr>
                <td>税込価格<br>
                (税抜価格)</td>
                <td><%= @item.price %></td>
              </tr>
            </tbody>
          </table>
        <% end %>
      </div>
  </div>
</div>